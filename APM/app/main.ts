import { bootstrap } from 'angular2/platform/browser';
// this is not importing from angular core because features are different depending on the platform

// Our main component
import { AppComponent } from './app.component';

bootstrap(AppComponent);
